{
  "description": "Tests the specified HTTP/S connection for the specified status code.",
  "parameters": {
    "http_port": {
      "type": "Optional[String[1]]",
      "description": "The port on the host to connect to (default 8080)."
    },
    "use_ssl": {
      "type": "Optional[Boolean]",
      "description": "Whether to use an SSL connection (default false)."
    },
    "test_path": {
      "type": "Optional[String[1]]",
      "description": "The path on the host to check (default /)."
    },
    "expected_code": {
      "type": "Optional[Integer]",
      "description": "The expected return status code (default 200)."
    },
    "verify_peer": {
      "type": "Optional[Boolean]",
      "description": "Whether to verify the peer credentials (default true)."
    }
  },
  "files": ["cd4pe/lib/"],
  "input_method": "stdin"
}