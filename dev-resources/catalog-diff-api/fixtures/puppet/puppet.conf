# Configure Puppet Server for tests
[master]
# Configure the indirector to store and load facts as YAML files under
# the $vardir.
fact_terminus=yaml

# Kick the SSL directory over to the $vardir so that tests don't leave
# cruft behind.
ssldir=$vardir/ssl
